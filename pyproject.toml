[project]
name = "myapp"
authors = [
    { name = "rgc318", email = "1511659008rgc@gmail.com"}
]
description = "new app"
requires-python = ">=3.14"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
    "rgc-backend-kit>=0.1.1,<0.2.0",
    # Frappe 16.18.x currently requires requests~=2.33.0.
    "requests>=2.33.0,<2.34.0",
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

# These dependencies are only installed when developer mode is enabled
[tool.bench.dev-dependencies]
# package_name = "~=1.1.0"
debugpy = ">=1.8.20"

[tool.ruff]
line-length = 110
target-version = "py314"

[tool.ruff.lint]
select = [
    "F",
    "E",
    "W",
    "I",
    "UP",
    "B",
    "RUF",
]
ignore = [
    "B017", # assertRaises(Exception) - should be more specific
    "B018", # useless expression, not assigned to anything
    "B023", # function doesn't bind loop variable - will have last iteration's value
    "B904", # raise inside except without from
    "E101", # indentation contains mixed spaces and tabs
    "E402", # module level import not at top of file
    "E501", # line too long
    "E741", # ambiguous variable name
    "F401", # "unused" imports
    "F403", # can't detect undefined names from * import
    "F405", # can't detect undefined names from * import
    "F722", # syntax error in forward type annotation
    "W191", # indentation contains tabs
    "UP030", # Use implicit references for positional format fields (translations)
    "UP031", # Use format specifiers instead of percent format
    "UP032", # Use f-string instead of `format` call (translations)
    "UP037", # quoted annotations
    "UP040", # Use type aliases instead of type annotations
    "RUF001", # Chinese business strings intentionally use full-width punctuation
    "RUF002", # Chinese docstrings intentionally use full-width punctuation
    "RUF003", # Chinese comments intentionally use full-width punctuation
    "I001", # Preserve the current import layout until a dedicated formatting-only migration
    "RUF005", # Iterable unpacking suggestions are non-functional style changes
    "RUF022", # Preserve public API export ordering until a dedicated formatting-only migration
    "RUF059", # HTTP tests intentionally keep selected request payloads for debugging symmetry
]
typing-modules = ["frappe.types.DF"]

[tool.ruff.format]
quote-style = "double"
indent-style = "tab"
docstring-code-format = true
